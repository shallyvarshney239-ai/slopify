/** Central route paths for the Slopify SPA. */
export const ROUTES = {
  HOME: '/',
  ANALYSIS: '/analysis',
  PR_ANALYSIS: '/analysis/pr',
  ACCURACY: '/accuracy',
}

/** Legacy paths kept for bookmarks and external links. */
export const LEGACY_ROUTES = {
  RESULTS: '/results',
  EVAL: '/eval',
}
